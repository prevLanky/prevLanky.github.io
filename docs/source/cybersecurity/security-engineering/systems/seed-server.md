# Seed server

This is how i build my seed server.

# What is a seed server?
A seed server is used as the initial server that is used when installing your infrastructure.
Once the infrastructure is up and running. The Seed server is most likely not needed, atleast no for general operations.

